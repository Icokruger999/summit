{
  "commands": [
    "cd /etc/nginx",
    "ls -la conf.d/ 2>/dev/null | grep api || echo 'Checking for api config...'",
    "grep -r 'api.codingeverest.com' conf.d/ 2>/dev/null | head -5 || echo 'No api config in conf.d'",
    "grep -r 'proxy_pass.*5001' /etc/nginx/ 2>/dev/null | grep -v '5000' || echo 'No 5001 references found'",
    "sudo find /etc/nginx -name '*api*' -o -name '*summit*' 2>/dev/null | head -5"
  ]
}

