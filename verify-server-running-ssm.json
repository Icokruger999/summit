{
  "commands": [
    "echo '=== Server Status Check ==='",
    "echo ''",
    "echo '1. PM2 Process Status:'",
    "export HOME=/root",
    "pm2 list",
    "echo ''",
    "echo '2. Port 3000 Status:'",
    "ss -tlnp 2>/dev/null | grep :3000 || netstat -tlnp 2>/dev/null | grep :3000 || echo 'Port 3000 NOT listening'",
    "echo ''",
    "echo '3. Health Endpoint Test:'",
    "curl -v http://localhost:3000/health 2>&1 | head -20",
    "echo ''",
    "echo '4. PM2 Process Info:'",
    "pm2 describe summit 2>&1 | head -30 || echo 'Process not found in PM2'",
    "echo ''",
    "echo '5. PM2 Error Logs (last 30 lines):'",
    "pm2 logs summit --err --lines 30 --nostream 2>&1 | tail -30 || echo 'No error logs'"
  ]
}
