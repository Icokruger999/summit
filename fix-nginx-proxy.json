{
  "commands": [
    "cat /etc/nginx/conf.d/summit-api.conf | grep -A 3 'location /api'",
    "sudo sed -i 's|proxy_pass http://127.0.0.1:4000;|proxy_pass http://127.0.0.1:4000/api;|g' /etc/nginx/conf.d/summit-api.conf",
    "grep proxy_pass /etc/nginx/conf.d/summit-api.conf",
    "sudo nginx -t",
    "sudo systemctl reload nginx",
    "echo '✅ Nginx routing fixed'"
  ]
}

