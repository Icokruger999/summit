{
  "commands": [
    "echo '=== PM2 Process Status ==='",
    "pm2 list",
    "echo ''",
    "echo '=== PM2 Logs (last 20 lines) ==='",
    "pm2 logs summit --lines 20 --nostream || echo 'No logs or process not found'",
    "echo ''",
    "echo '=== Port 3000 Status ==='",
    "netstat -tlnp 2>/dev/null | grep :3000 || ss -tlnp 2>/dev/null | grep :3000 || echo 'Port 3000 NOT listening'",
    "echo ''",
    "echo '=== Environment File Check ==='",
    "cd /var/www/summit/server && cat .env | grep PORT || echo 'PORT not in .env'",
    "echo ''",
    "echo '=== Testing Health Endpoint ==='",
    "curl -v http://localhost:3000/health 2>&1 | head -20 || echo 'Health endpoint failed'",
    "echo ''",
    "echo '=== Process Check ==='",
    "ps aux | grep 'node.*index.js' | grep -v grep || echo 'No node process found'"
  ]
}

