version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Deploying static HTML files"
        - ls -la
        - test -f index.html && echo "✅ index.html found" || echo "❌ index.html missing"
  artifacts:
    files:
      - 'index.html'
      - 'app/**/*'
  cache:
    paths: []

