version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Summit Login - No build needed (static HTML)"
    build:
      commands:
        - echo "Deploying static files"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths: []

