{
  "InstanceIds": ["i-0fba58db502cc8d39"],
  "DocumentName": "AWS-RunShellScript",
  "Parameters": {
    "commands": [
      "cd /var/www/summit/server",
      "echo 'Checking if dist folder exists...'",
      "ls -la dist/ 2>&1 | head -5 || echo 'dist folder missing'",
      "echo ''",
      "echo 'Checking if index.js exists...'",
      "ls -la dist/index.js 2>&1 || echo 'index.js missing'",
      "echo ''",
      "echo 'Trying to start manually to see error...'",
      "cd /var/www/summit/server && node dist/index.js 2>&1 | head -20 || echo 'Failed to start'"
    ]
  }
}

