{
  "commands": [
    "sudo mv /etc/nginx/conf.d/api.codingeverest.com.conf /etc/nginx/conf.d/00-summit-api.conf",
    "grep 'proxy_pass' /etc/nginx/conf.d/00-summit-api.conf",
    "sudo nginx -t",
    "sudo systemctl reload nginx",
    "echo '✅ Summit config renamed to load first (Milo untouched)'"
  ]
}

