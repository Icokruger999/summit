{
  "commands": [
    "pm2 list | grep summit || echo 'PM2 process not found'",
    "curl -s http://localhost:4000/health || echo 'Health endpoint failed'",
    "curl -s http://localhost:4000/api/summit/status || echo 'Summit API endpoint failed'"
  ]
}

